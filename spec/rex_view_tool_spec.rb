require "spec_helper"

describe RexViewTool do
  it "has a version number" do
    expect(RexViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
